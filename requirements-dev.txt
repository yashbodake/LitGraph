# Extra dependencies for the full build / evaluation pipeline:
# data download, chunking (HDBSCAN), NER ingestion (SciSpacy), and eval.
# Installs the runtime set first, then the build/eval extras:
#   pip install -r requirements-dev.txt

-r requirements.txt

# Data download
datasets>=2.14.0
huggingface-hub>=0.20.0

# Chunking (semantic clustering)
hdbscan>=0.8.33
scikit-learn>=1.3.0
nltk>=3.8.1
umap-learn>=0.5.4

# NER ingestion (SciSpacy). Install the model separately:
#   pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz --no-deps
scispacy>=0.5.4

# Evaluation
rouge-score>=0.1.2
bert-score>=0.3.13

# Build-time visualisation / reports
matplotlib>=3.7.0
plotly>=5.17.0
networkx>=3.1
pandas>=2.0.0
tqdm>=4.66.0
