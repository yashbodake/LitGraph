# Runtime dependencies for the GraphRAG demo app (app/demo.py).
# This is the minimal set Streamlit Community Cloud installs. Build/eval-only
# deps (scispacy, datasets, bert-score, hdbscan, umap, etc.) live in
# requirements-dev.txt and are NOT needed to run the demo.

# UI
streamlit>=1.30.0

# Knowledge graph
neo4j>=5.14.0

# Embeddings (vector search) — pulls torch as its backend
sentence-transformers>=2.6.0
torch>=2.0.0

# LLM client (Cerebras is OpenAI-compatible)
openai>=1.12.0
requests>=2.31.0

# Visualisation
pyvis>=0.3.2

# Data / utils
numpy>=1.24.0
python-dotenv>=1.0.0
